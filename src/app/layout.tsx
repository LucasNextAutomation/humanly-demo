import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Humanly — Off-Market Land Acquisition System",
  description: "Land Intelligence Engine + Outreach & Deal Pipeline for vacant land acquisition across Florida MSAs. Built for Humanly by NextAutomation.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className={`${inter.className} antialiased`}>
        {children}
      </body>
    </html>
  );
}
